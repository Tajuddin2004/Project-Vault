# Docker templates

These are platform-owned templates selected only after manifest detection. Do not build arbitrary student-supplied Dockerfiles, run privileged containers, mount the host Docker socket, use host networking, or expose container ports directly to the internet.
